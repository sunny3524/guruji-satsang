# Port that the Express API server will listen on
PORT=3001

# Shared secret key used to authenticate Firebase Cloud Functions requests
# The Cloud Function sends this in the 'Authorization: Bearer <secret>' header
# IMPORTANT: Change this to a secure, long random string in production!
WHATSAPP_API_SECRET=your_super_secret_otp_token_here_98765
